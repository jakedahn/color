[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run dist"
  publish = "dist"
  functions = "functions"

[context.production]
  [context.production.environment]
    NODE_ENV = "production"

[dev]
  autoLaunch = false
